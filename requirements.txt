# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Supabase client (wraps PostgREST + Storage + Auth)
supabase>=2.4.0

# LLM providers
openai>=1.30.0
anthropic>=0.28.0

# Configuration
python-dotenv>=1.0.0

# PDF extraction (optional — install at least one)
pypdf>=4.2.0
# pdfminer.six>=20221105  # heavier alternative; uncomment if needed

# LangGraph — stateful multi-agent workflows with recursive evidence drilling
langgraph>=0.2.0
langchain-core>=0.3.0

# mem0 — persistent memory layer for PM agents (cross-session recall)
mem0ai>=0.1.0
